<h1>Driver Page</h1>

<h4><%= link_to "(Add New Driver)", new_driver_path %></h4>


<ul>
    <% @drivers.each do |driver| %>
    <%= link_to "(#{driver.name})", driver_path(driver.id) %>
      <!-- create average rating method in the model: calling it on each instance of the driver  -->
      <%= link_to "Edit", edit_driver_path(driver.id) %>
         <%= link_to "Delete", driver_path(driver), method: :delete, data: { confirm: "Are you 1000% sure you want to delete the passenger #{driver.name}?" } %>

    <p>
    <% end %>
</ul>
