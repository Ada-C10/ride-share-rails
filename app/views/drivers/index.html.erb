<h1>Drivers List</h1>

<ul>
  <% @drivers.each do |driver| %>
  <li class="driver">
    <span class="name">
      <%=  link_to driver.name, driver_path(driver.id), class: 'driver-link' %>
    </span>
    VIN: <%= driver.vin %>
  </li>
  <% end %>
</ul>
