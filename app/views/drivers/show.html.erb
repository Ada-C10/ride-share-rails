
  <h1>Driver<%="#{@driver.name}"%></h1>

  <h1><%= @driver.name %></h1>
  <h1><%= @driver.vin %> </h1>
  <%= link_to "Edit", edit_driver_path(@driver.id) %>
  <div></div>
  <%= link_to "Delete", driver_path(@driver.id), method: :delete, data: {confirm: "Please confirm you want to be deleted"} %>
<%=@driver.total_revenue%>
