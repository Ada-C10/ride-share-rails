<h2>Driver Details</h2>


<p>
  <strong>
    <%= @driver.name %>
    <%= @driver.id %>
    <%= @driver.vin %>
  </strong>

</p>

<div class="links">

  <%= link_to "delete ", @drivers, method: :delete, data: { confirm: "Are you really really sure that you want to delete ?" } %>
  <%= link_to "Edit Drivers ", edit_passenger_path(@drivers.id) %>

</div>
