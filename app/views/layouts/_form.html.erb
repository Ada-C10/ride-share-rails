<%= form_with model: model, url: url, method: method do |f| %>
  <%= message %>
  <%= f.label field_one_text_field, field_one_label %>
  <%= f.text_field field_one_text_field, placeholder: field_one_placeholder, required: true %>

  <!-- TODO: Add cool features -->
  <!-- maxlength: number, size: number, pattern: regex -->
  <!-- auto focus -->

  <%= f.label field_two_text_field, field_two_label %>
  <%= f.text_field field_two_text_field, placeholder: field_two_placeholder, required: true, maxlength: field_two_maxlength, pattern: field_two_pattern %>

  <%= f.submit button_text %>

<% end %>
