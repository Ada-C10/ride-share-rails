<% if model.errors.any? %>
 <ul class = "errors">
    <% model.errors.each do |column, message| %>
      <li>
        <strong><%= column.capitalize %></strong>
        <%= message %>
      </li>
    <% end %>
  </ul>
<% end %>
