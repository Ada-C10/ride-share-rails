<section class = "errors">
<% if model.errors.any? %>
<p>   >> There was an error in saving your submission. Please review details below and try again.
</p>

<ul class>
  <% model.errors.each do |column, message| %>
  <li>
    <strong><%= column.capitalize %></strong>
    <%= message %>
  </li>
  <% end %>
</ul>
<% end %>
</section>
