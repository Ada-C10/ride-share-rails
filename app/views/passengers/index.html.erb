<section class='passengers-list'>
<h1>Here's who's riding</h1>
<% @passengers.each do |passenger| %>
  <h2 class='list-it'><%= link_to "#{passenger.name}", passenger_path(passenger.id) %>
  </h2>
  <% end %>
</section>
