<h1>PASSENGERS</h1>

<table>
<tr><strong><td>Passenger Name</td><td>Phone Number</td></strong></tr>
<% Passenger.active_passenger.each do |passenger| %>
  <tr>
  <td><%= link_to "#{passenger.name}", passenger_path(passenger.id) %></td>
  <td><%= passenger.phone_num %></td>
</tr>
<% end %>
</table>
