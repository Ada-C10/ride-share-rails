<!-- <h1>Passengers#show</h1>
<p>Find me in app/views/passengers/show.html.erb</p> -->

<% if @passenger.nil? %>
  <h1> 404 Not found</h1>
<% else %>
  <h1><%=@passenger.name %></h1>
  <table>
    <tr>
      <th>ID</th>
      <th>Phone Number</th>
      <th>Total Spent</th>
    </tr>
    <tr>
      <td><%= "#{@passenger.id}" %></td>
      <td><%= "#{@passenger.phone_num}" %></td>
      <td><%= "$#{@passenger.total_charged}" %></td>  
    </tr>

  </table>

  <%= link_to "Edit", edit_passenger_path(@passenger.id) %>
  <!--TODO FIgure out how to delete passengers without impacting driver trips.   -->
  <!--  Maybe set passenger id to null then delete? -->
  <%= link_to "Delete #{@passenger.name}", passenger_path(@passenger.id), method: :delete %>
<% end %>
