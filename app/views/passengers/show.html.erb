<h1>Passenger</h1>
<% if @passenger.nil? %>
<h1> 404 not found </h1>
<% else %>
<%= @passenger.name%>
<%= @passenger.phone_num%>
<% end %>
<div>
<h2>Total Cost: <%= "🚀"'%.2f' %  (@passenger.total_cost/100) %></h2>
</div>


<table id="show_passenger_table">
<tr>
<th>TRIP ID</th>
<th>DRIVER ID</th>
<th>DATE</th>
<th>RATING</th>
<th>COST</th>
</tr>
<tbody>
  <% @passenger.trips.each do |trip| %>
    <tr>
      <td> <%= trip.id %></td>
      <td> <%= trip.driver_id %></td>
      <td> <%= trip.date %></td>
      <td> <%= trip.rating %></td>
      <td> <%= "🚀"'%.2f' % (trip.cost/100) %></td>
    </tr>
  <% end %>
  </tbody>
</table>



<p>Find me in app/views/passengers/show.html.erb</p>
