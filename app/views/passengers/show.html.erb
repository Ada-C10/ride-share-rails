<!-- <h1>Passengers#show</h1>
<p>Find me in app/views/passengers/show.html.erb</p> -->

<% if @passenger.nil? %>
  <h1> 404 Not found</h1>
<% else %>
  <h1><%=@passenger.name %></h1>
  <h3> by <%= link_to "#{@passenger.name}", passengers_show_path(@passenger.id) %></h3>
  <p><%= @passenger.phone_num %> </p>
  <%= link_to "Edit", passengers_edit_path(@passenger.id) %>
  <div></div>
  <%= link_to "Delete #{@passenger.name}", passengers_destroy_path(@passenger.id), method: :delete %>
<% end %>
