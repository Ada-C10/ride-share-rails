<h2>Trips List</h2>
<%= will_paginate @trips %>
<ul>
  <% @trips.each do |trip| %>
  <li class="trip">
    <span class="name">
      <%= trip.id %>
      <%=  link_to trip.date, trip_path(trip.id), class: 'name-link' %>
    </span>
  </li>
  <% end %>
</ul>
