<h1>Trip Details</h1>

  <h2> Details for trip taken on <%=" #{@trip.date.strftime("%b %d, %Y")}" %> </h2>
  <h3><%="This trip cost $ #{@trip.cost}"%></h3>
  <h3> The driver was: <%= link_to  " #{Driver.find(@trip.driver_id).name}", driver_path(@trip.driver_id)%></h3>
  <h3>  The rider was: <%= link_to  " #{Passenger.find(@trip.passenger_id).name}", passenger_path(@trip.passenger_id)%></h3>
  <h3> <%= "The rating is #{@trip.rating}"%></h3>

  <%= link_to "Edit", edit_trip_path(@trip.id) %>
